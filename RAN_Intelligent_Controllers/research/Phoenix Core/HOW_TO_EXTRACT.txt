-------------------------------------------------------------------------------

To unzip it:

zip -s 0 phoenix-repo-encrypted.zip --out combined-phoenix-repo-encrypted
unzip -P 'PASTE_PASSWORD_HERE' combined-phoenix-repo-encrypted.zip
rm -rf combined-phoenix-repo-encrypted.zip
mv phoenix-repo ../../../5G_Core_Network/Additional_Cores_5GDeploy/
cd ../../../5G_Core_Network/Additional_Cores_5GDeploy/

-------------------------------------------------------------------------------

To re-zip it:

zip -r -P 'PASTE_PASSWORD_HERE' -s 90m phoenix-repo-encrypted.zip phoenix-repo

-------------------------------------------------------------------------------
