diff --git a/srsue/src/stack/mac_nr/test/proc_ra_nr_test.cc b/srsue/src/stack/mac_nr/test/proc_ra_nr_test.cc
index 2bee0b745..f74cb4a3f 100644
--- a/srsue/src/stack/mac_nr/test/proc_ra_nr_test.cc
+++ b/srsue/src/stack/mac_nr/test/proc_ra_nr_test.cc
@@ -121,7 +121,7 @@ int proc_ra_normal_test()
   int      preamble_received_target_power = 0;
   dummy_phy.get_last_send_prach(&prach_occasion, &preamble_index, &preamble_received_target_power);
   TESTASSERT(prach_occasion == 0);
-  TESTASSERT(preamble_index == 0);
+  TESTASSERT(preamble_index < 64);
   TESTASSERT(preamble_received_target_power == -110);
   // Simulate PHY and call prach_sent (random values)
   uint32_t tti_start = 0;
@@ -183,13 +183,19 @@ int proc_ra_timeout_test()
   int      preamble_received_target_power = 0;
   dummy_phy.get_last_send_prach(&prach_occasion, &preamble_index, &preamble_received_target_power);
   TESTASSERT(prach_occasion == 0);
-  TESTASSERT(preamble_index == 0);
+  TESTASSERT(preamble_index < 64);
   TESTASSERT(preamble_received_target_power == -110);
   // Simulate PHY and call prach_sent (random values)
   uint32_t tti = 0;
 
   for (uint32_t j = 0; j < rach_cfg.preambleTransMax; j++) {
     proc_ra_nr.prach_sent(tti, 7, 1, 0, 0);
+    dummy_phy.get_last_send_prach(&prach_occasion, &preamble_index, &preamble_received_target_power);
+    TESTASSERT(prach_occasion == 0);
+    TESTASSERT(preamble_index < 64);
+    const int expected_target_power =
+        rach_cfg.PreambleReceivedTargetPower + static_cast<int>(j * rach_cfg.powerRampingStep);
+    TESTASSERT(preamble_received_target_power == expected_target_power);
     uint32_t i = 0;
     for (i = tti; i < tti + rach_cfg.ra_responseWindow + 100; i++) {
       // update clock and run internal tasks
